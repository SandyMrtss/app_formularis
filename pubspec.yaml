name: app_formularis
description: "An easy app to start using forms"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_form_builder: ^9.5.0
  form_builder_validators: ^11.0.0
  form_builder_extra_fields: ^11.0.0
  cupertino_icons: ^1.0.8
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true